<h1> <%= @restaurant.name %> </h1>
<p> <%= @restaurant.address %> </p>
<p> <%= @restaurant.category %> </p>

<div>
  <p>Rating:</p>

  <ul>
    <% @restaurant_reviews.each do |review|%>
    <li> <%= review.content %>
    <% review.rating.times do %>
    <span>⭐️</span>
    <% end %>
    </li>
    <% end %>
    </ul>
</div>


<h1>Provide Your feedback here</h1>

<p>Your Honst Feedback Would be Appriciated👨🏿‍🍳</p>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
<% end %>
<%= link_to "Back to Main Page", restaurants_path %>
